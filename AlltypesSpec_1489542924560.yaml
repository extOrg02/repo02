{
  "definitions": {
    "HelloMsg": {
      "properties": {
        "Hello": {
          "default": "Nikhil",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  "info": {
    "description": "This is APISpec is created before 190 release which has no patch method",
    "title": "Pre180APISpecWithoutPatch",
    "version": "1.2.3",
    "x-lastModified": "May 01, 2017 23:13PM PST"
  },
  "paths": {
    "/comp/{name}": {
      "get": {
        "description": "Computer Store",
        "parameters": [
          {
            "name": "name",
            "in": "path",
            "description": "",
            "required": true,
            "type": "string",
            "format": ""
          }
        ],
        "produces": [
          "application/json"
        ],
        "responses": {
          "200": {
            "description": "Success response",
            "examples": {
              "application/json": {
                "Hello": "Nikhil"
              }
            },
            "schema": {
              "$ref": "#/definitions/HelloMsg"
            }
          }
        }
      }
    }
  },
  "swagger": "2.0"
}